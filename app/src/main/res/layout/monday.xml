<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/messmenuback"
    android:orientation="vertical"
    tools:context=".mainMessMenu$PlaceholderFragment">

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10sp"
        android:layout_marginTop="10sp"
        android:text="@string/h1"
        android:textAlignment="center"
        android:textAppearance="@style/Base.Widget.AppCompat.Spinner.Underlined"
        android:textColor="@android:color/black"
        android:textSize="35sp"
        android:textStyle="bold" />

    <ListView
        android:id="@+id/listView1"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="5sp"
        tools:layout_marginBottom="10dp"
        tools:layout_marginLeft="30dp"
        tools:layout_marginRight="30dp"></ListView>

</LinearLayout>